// Example components/Header.js
import React from 'react';

const Header = () => {
   return (
      <header>
         <h1>ATS-OpenSource</h1>
         {/* Add navigation or other header elements */}
      </header>
   );
};

export default Header;
